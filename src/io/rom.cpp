#include "./rom.hpp"

// Reader::Reader(const std::string &filename) {
//   _file.open(filename, std::ios::binary);
//   _file.read(reinterpret_cast<char *>(_rom.data()), _rom.size());
//   _file.close();
// }

// uint8_t Reader::read(uint16_t addr) const { return _rom[addr]; }